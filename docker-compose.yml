version: '3.8'

services:
  web_app:
    image: nginx:alpine
    container_name: uas_web_app
    volumes:
      - ./html:/usr/share/nginx/html
    expose:
      -
    ports:
      - "8085:80"
    volumes:
      - ./html:/usr/share/nginx/html
      - ./data:/app/data
    restart: always