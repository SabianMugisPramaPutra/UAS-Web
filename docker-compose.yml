version: '3.8'

services:
  web-app:
    image: nginx:alpine
    container_name: uas-web-app
    ports:
      - "8085:80"
    volumes:
      - ./html:/usr/share/nginx/html
      - ./data:/app/data
    restart: always